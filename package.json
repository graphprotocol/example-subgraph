{
  "name": "cosmos-event-example",
  "version": "0.1.0",
  "repository": "https://github.com/figment-networks/example-subgraph",
  "license": "MIT",
  "scripts": {
    "create": "graph create example --node https://api.thegraph.com/deploy/",
    "create-local": "graph create cosmos-example --node http://localhost:8020",
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy example --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy-local": "graph deploy cosmos-example -l v0.1.0 --ipfs http://localhost:5001 --node http://localhost:8020",
    "remove-local": "graph remove cosmos-example --node http://localhost:8020"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "https://github.com/figment-networks/graph-cli.git#tendermint",
    "@graphprotocol/graph-ts": "https://github.com/figment-networks/graph-ts.git#feature/GRAPH-306"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0",
    "pretty-error": "^4.0.0",
    "as-proto": "^0.2.2"
  }
}
