{
  "name": "cosmos-block-example",
  "version": "0.1.0",
  "repository": "https://github.com/graphprotocol/example-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create cosmos-block-example --node http://localhost:8020",
    "deploy-local": "graph deploy cosmos-block-example -l v0.1.0 --ipfs http://localhost:5001 --node http://localhost:8020",
    "remove-local": "graph remove cosmos-block-example --node http://localhost:8020",
    "create": "graph create cosmos-block-example --node https://api.thegraph.com/deploy/",
    "deploy": "graph deploy cosmos-block-example --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "https://github.com/figment-networks/graph-cli.git#cosmos",
    "@graphprotocol/graph-ts": "https://github.com/figment-networks/graph-ts.git#cosmos"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0"
  }
}
