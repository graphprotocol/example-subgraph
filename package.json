{
  "name": "example",
  "version": "0.1.0",
  "repository": "https://github.com/graphprotocol/example-subgraph",
  "license": "MIT",
  "scripts": {
    "build-contract": "solc contracts/Gravity.sol --abi -o abis --overwrite && solc contracts/Gravity.sol --bin -o bin --overwrite",
    "create": "graph create example --node https://api.thegraph.com/deploy/",
    "create-local": "graph create example --node http://127.0.0.1:8020",
    "create-test": "graph create example --node http://graph-node:8020",
    "remove-test": "graph remove example --node http://graph-node:8020",
    "codegen": "graph codegen --output-dir src/types/",
    "build": "graph build --debug",
    "deploy": "graph deploy example --ipfs https://api.thegraph.com/ipfs/ --node https://api.thegraph.com/deploy/",
    "deploy-local": "graph deploy example --ipfs http://localhost:5001 --node http://127.0.0.1:8020",
    "deploy-test": "graph deploy example --ipfs http://ipfs:5001 --node http://graph-node:8020",
    "test": "truffle test --network test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.9.0",
    "@graphprotocol/graph-ts": "^0.9.0",
    "apollo-fetch": "^0.7.0",
    "gluegun": "^3.2.0"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "babel-register": "^6.26.0",
    "truffle": "^5.0.4",
    "truffle-contract": "^4.0.5",
    "truffle-hdwallet-provider": "^1.0.4"
  }
}
